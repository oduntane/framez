
describe('postService', () => {
    describe('uploadImage()', () => {
        it('should call supabase storage upload with correct file', () => {

        })

        it('should return public URL on success', () => {
            
        })

        it('should throw error on upload failure', () => {

        })
    })

    describe('createPost', () => {
        it('should call supabase insert with correct data', () => {

        })

        it('should return created post object', () => {

        })

        it('should throw error if userId is missing', () => {

        })

        it('should throw error if userId is missing', () => {

        })

        it('should throw error if text is empty', () => {
            
        })
    })
})